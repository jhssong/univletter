spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false

  devtools:
    add-properties: false

server:
  port: ${PORT}
  forward-headers-strategy: native
  servlet:
    session:
      cookie:
        secure: true
        http-only: true
        same-site: none
        domain: "jhssong.com"

admin:
  email: ${ADMIN_EMAIL}
  password: ${ADMIN_PASSWORD}
  api-key: ${ADMIN_API_KEY}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
